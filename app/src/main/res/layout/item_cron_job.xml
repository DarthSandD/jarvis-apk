<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/spacing_sm"
    app:cardCornerRadius="@dimen/corner_card"
    app:cardBackgroundColor="@color/jarvis_surface_elevated"
    app:strokeColor="@color/jarvis_divider"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_md">

        <!-- Top row: name + status badge -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <View
                android:id="@+id/cron_status_dot"
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:layout_marginEnd="@dimen/spacing_sm"
                android:background="@drawable/arc_reactor_core" />

            <TextView
                android:id="@+id/txt_cron_name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/jarvis_text_primary"
                android:textSize="@dimen/text_body"
                android:fontFamily="sans-serif-medium" />

            <com.google.android.material.chip.Chip
                android:id="@+id/chip_cron_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="ACTIVE"
                android:textColor="@color/jarvis_online"
                android:textSize="@dimen/text_tiny"
                app:chipBackgroundColor="@color/jarvis_chip_background"
                app:chipCornerRadius="@dimen/corner_pill"
                app:ensureMinTouchTargetSize="false" />
        </LinearLayout>

        <!-- Schedule + last run -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="@dimen/spacing_xs">

            <TextView
                android:id="@+id/txt_cron_schedule"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textColor="@color/jarvis_text_secondary"
                android:textSize="@dimen/text_caption" />

            <TextView
                android:id="@+id/txt_cron_last_run"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/jarvis_text_hint"
                android:textSize="@dimen/text_caption" />
        </LinearLayout>

        <!-- Action buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end"
            android:layout_marginTop="@dimen/spacing_sm">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_cron_run"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/spacing_xs"
                android:text="Run"
                android:textColor="@color/jarvis_online"
                android:textSize="@dimen/text_tiny"
                app:backgroundTint="@color/jarvis_chip_background"
                app:strokeColor="@color/jarvis_online"
                app:strokeWidth="1dp"
                app:cornerRadius="@dimen/corner_pill"
                app:ensureMinTouchTargetSize="false"
                app:icon="@drawable/ic_send"
                app:iconTint="@color/jarvis_online"
                app:iconSize="14dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_cron_toggle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Pause"
                android:textColor="@color/jarvis_gold"
                android:textSize="@dimen/text_tiny"
                app:backgroundTint="@color/jarvis_chip_background"
                app:strokeColor="@color/jarvis_gold_dim"
                app:strokeWidth="1dp"
                app:cornerRadius="@dimen/corner_pill"
                app:ensureMinTouchTargetSize="false" />
        </LinearLayout>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
